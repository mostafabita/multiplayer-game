<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <title>{{htmlWebpackPlugin.options.title}}</title>

  <link rel="shortcut icon" type="image/png" href="favicon.png" />
  <link rel="apple-touch-icon" href="favicon.png" />

  {{#each htmlWebpackPlugin.options.files.css}}
  <link href="{{this}}" rel="stylesheet" />
  {{/each}}
</head>

<body>
  <div class="container-lg d-flex flex-column justify-content-end h-100 mw-fixed p-4">
    <header class="d-flex justify-content-between align-items-center mb-auto">
      <h3 class="m-0 text-white">
        {{htmlWebpackPlugin.options.title}}
        <small>—</small>
        <small class="current-username">{{htmlWebpackPlugin.options.subtitle}}</small>
      </h3>
      <nav class="nav justify-content-center">
        <a class="nav-link active" href>Game</a>
        <a class="nav-link" target="_blank" href="{{htmlWebpackPlugin.options.contact}}">Contact</a>
      </nav>
    </header>

    <main role="main">
      {{!-- Startup section --}}
      <section id="startupSection">
        <form id="startupForm">
          <h1 class="mb-3 text-white">Put your name _</h1>
          <div class="input-group mb-3">
            <div class="input-group-prepend">
              <span class="input-group-text boy-image"></span>
            </div>
            <input id="usernameInput" type="text" autofocus autocomplete="off" class="form-control form-control-lg"
              placeholder="here ..." maxlength="15" pattern="[a-zA-Z0-9]+" required>
          </div>
          <button type="submit" class="btn btn-lg btn-outline-light">Let's go</button>
        </form>
      </section>
      {{!-- Warmup section --}}
      <section id="warmupSection">
        <div class="d-flex">
          {{!-- <img src="assets/snake.svg" alt="Snake" width="50"> --}}
          <h1 class="mb-3 text-white">~ Snake Playground </h1>
        </div>
        <div class="d-flex flex-row columns shadow-lg">
          {{!-- Messages --}}
          <div class="message-box-container mx-1 d-flex flex-column justify-content-between">
            <ul id="messageList" class="list-group message-list"></ul>
            <form id="messageForm">
              <input id="messageInput" type="text" autocomplete="off" required class="form-control"
                placeholder="Chat here ...">
            </form>
          </div>
          {{!-- Game --}}
          <div class="game-container mx-1 d-flex flex-fill">
            <canvas id="playground"></canvas>
          </div>
        </div>
      </section>
    </main>

    <footer class="mt-auto">
      <div class="text-center">
        Created by
        <a target="_blank" class="text-white" href="{{htmlWebpackPlugin.options.contact}}">@mostafabita</a>
      </div>
    </footer>
  </div>

  {{#each htmlWebpackPlugin.options.files.js}}
  <script src="{{this}}" type="text/javascript"></script>
  {{/each}}
</body>

</html>